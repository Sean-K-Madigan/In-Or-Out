  <div class="grid-container">
  <div class="card animate__animated animate__flipInY">
    <h2 class="title">{{title}}</h2>
    <p class="creator">Created by: {{created_by}}</p>
    <p class="category">{{category}}</p>
    <p class="description">{{description}}</p>
    <p class="participant-limit">Participant limit: {{participantLimit}}</p>
    <div class="joinDeleteBtns d-flex flex-row justify-content-around">
      <button id="join-button" class="join-button rounded-pill p-1 submit" data-id="{{id}}" data-user="{{user_id}}">Join</button>
      {{!-- getting data from homeRoutes.js get all function, pasing user_id to join.js --}}
      <button id="delete-btn" class="delete rounded-pill p-1" data-id="{{id}}">Hide</button>
    </div>
  </div>

  </div>